[workspace]
members = [
    "main",
    "core"
]
resolver = "3"

[profile.dev]
panic = "abort"
strip = true
debug = false

[profile.release]
opt-level = 3
panic = "abort"
lto = "fat"
codegen-units = 1
strip = true
debug = false